// Intentionally empty. The active health route lives at /app/api/health/route.ts
// because this repo uses a root-level `app/` directory (Next.js ignores
// `src/app` when a root `app` exists). Kept only to avoid a dangling import.
export {};
